<view class="container">
  <scroll-view class="chat-list" scroll-y scroll-into-view="{{toView}}">
    <view class="chat-item {{item.type === 'user' ? 'user' : 'ai'}}" wx:for="{{msgList}}" wx:key="index" id="msg-{{index}}">
      <view class="avatar">{{item.type === 'user' ? '👤' : '🤖'}}</view>
      <view class="content">
        <text>{{item.content}}</text>
      </view>
    </view>
  </scroll-view>

  <view class="input-area">
    <input class="input" placeholder="请输入您的问题..." value="{{inputValue}}" bindinput="onInput" confirm-type="send" bindconfirm="sendMsg" />
    <button class="send-btn" bindtap="sendMsg" disabled="{{!inputValue}}">发送</button>
  </view>
</view>
